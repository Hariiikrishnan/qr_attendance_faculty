/* 🎨 App Color Palette
   - Minimal
   - Consistent UI
   - Easy theming later
*/

export const AppColors = {
  // Primary blues
  primaryBlue: "#6FA8DC",
  secondaryBlue: "#B9D6F2",

  // Background
  background: "#F4F8FC",

  // Glass / card surfaces
  glassWhite: "rgba(255, 255, 255, 0.8)",

  // Text
  darkText: "#1F2937",
  mutedText: "#6B7280",

  // Accent
  accentBlue: "#4F8EDC",
};

/* 📐 Common layout constants */
export const AppSpacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
};

/* 🔲 Border radius values */
export const AppRadius = {
  sm: 8,
  md: 12,
  lg: 16,
};

/* 🕒 Durations (ms) */
export const AppDurations = {
  fast: 150,
  normal: 250,
};
